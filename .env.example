# Documentation only; Flutter does not automatically load this file.
# Pass public configuration using --dart-define.
APP_ENV=demo
API_BASE_URL=https://api.example.invalid/v1
# No provider secrets, signing keys or payment secrets in the app.
